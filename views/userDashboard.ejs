<!DOCTYPE html>
<html>
    <head>
        <title><%= title %></title>
        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
        <link href="/css/style.css" rel="stylesheet">
        <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
    </head>
    <body>
        <%- include('partials/navbar') %>

        <div class="container py-5">
            <div class="row">
                <!-- User Profile Sidebar -->
                <div class="col-md-3">
                    <div class="card border-0 shadow-sm">
                        <div class="card-body text-center">
                            <div class="mb-3">
                                <img src="/images/<%= user.profile_picture || 'defaultProfilePicture.png' %>" 
                                     alt="Profile" class="rounded-circle" 
                                     style="width: 80px; height: 80px; object-fit: cover; border: 3px solid #007bff;"
                                     onerror="this.src='https://via.placeholder.com/80x80/007bff/ffffff?text=<%= user.username.charAt(0).toUpperCase() %>'">
                            </div>
                            <h5 class="card-title text-light"><%= user.username %></h5>
                            <p class="card-text text-muted">
                                <span class="badge bg-primary">Member</span>
                            </p>
                            <p class="small text-muted"><%= user.email %></p>
                            <div class="d-grid gap-2">
                                <a href="/editProfile" class="btn btn-outline-primary btn-sm">Edit Profile</a>
                                <a href="/bookings" class="btn btn-outline-success btn-sm">View All Bookings</a>
                                <a href="/billings" class="btn btn-outline-warning btn-sm">Billing History</a>
                            </div>
                        </div>
                    </div>
                </div>
                
                <!-- Main Dashboard Content -->
                <div class="col-md-9">
                    <div class="d-flex justify-content-between align-items-center mb-4">
                        <h1 class="h3 text-primary fw-bold">My Dashboard</h1>
                        <span class="text-light">Welcome back, <%= user.username %>!</span>
                    </div>

                    <% if (messages && messages.length > 0) { %>
                        <div class="alert alert-success border-0 rounded-3 mb-4">
                            <% messages.forEach(function(message) { %>
                                <p class="mb-0"><i class="bi bi-check-circle-fill me-2"></i><%= message %></p>
                            <% }); %>
                        </div>
                    <% } %>

                    <!-- Quick Stats Cards -->
                    <div class="row g-4 mb-5">
                        <div class="col-md-4">
                            <div class="card border-0 shadow-sm bg-primary text-white">
                                <div class="card-body text-center">
                                    <div class="display-6 mb-2">📅</div>
                                    <h5 class="card-title">My Bookings</h5>
                                    <h3 class="fw-bold"><%= bookings ? bookings.length : 0 %></h3>
                                    <p class="card-text">Recent bookings</p>
                                </div>
                            </div>
                        </div>
                        
                        <div class="col-md-4">
                            <div class="card border-0 shadow-sm bg-success text-white">
                                <div class="card-body text-center">
                                    <div class="display-6 mb-2">🏋️</div>
                                    <h5 class="card-title">Available Classes</h5>
                                    <h3 class="fw-bold"><%= upcomingClasses ? upcomingClasses.length : 0 %></h3>
                                    <p class="card-text">Classes to book</p>
                                </div>
                            </div>
                        </div>
                        
                        <div class="col-md-4">
                            <div class="card border-0 shadow-sm bg-warning text-dark">
                                <div class="card-body text-center">
                                    <div class="display-6 mb-2">💳</div>
                                    <h5 class="card-title">Recent Bills</h5>
                                    <h3 class="fw-bold"><%= billings ? billings.length : 0 %></h3>
                                    <p class="card-text">Recent transactions</p>
                                </div>
                            </div>
                        </div>
                    </div>

                    <!-- Recent Bookings Section -->
                    <div class="row g-4">
                        <div class="col-md-6">
                            <div class="card border-0 shadow-sm">
                                <div class="card-header bg-light">
                                    <h5 class="mb-0"><i class="bi bi-calendar-check me-2"></i>Recent Bookings</h5>
                                </div>
                                <div class="card-body">
                                    <% if (bookings && bookings.length > 0) { %>
                                        <% bookings.forEach(function(booking) { %>
                                            <div class="d-flex justify-content-between align-items-center border-bottom py-2">
                                                <div>
                                                    <h6 class="mb-1 text-light"><%= booking.class_name %></h6>
                                                    <small class="text-muted">
                                                        <i class="bi bi-geo-alt me-1"></i><%= booking.location_name %> - <%= booking.room_name %>
                                                    </small><br>
                                                    <small class="text-muted">
                                                        <i class="bi bi-clock me-1"></i><%= new Date(booking.class_start_time).toLocaleDateString() %>
                                                    </small>
                                                </div>
                                                <span class="badge bg-<%= booking.status === 'Booked' ? 'success' : booking.status === 'Cancelled' ? 'danger' : 'primary' %>">
                                                    <%= booking.status %>
                                                </span>
                                            </div>
                                        <% }); %>
                                        <div class="text-center mt-3">
                                            <a href="/bookings" class="btn btn-outline-primary">View All Bookings</a>
                                        </div>
                                    <% } else { %>
                                        <div class="text-center py-4">
                                            <div class="display-6 text-muted mb-3">📅</div>
                                            <h6 class="text-muted">No bookings yet</h6>
                                            <p class="text-muted">Book your first class to get started!</p>
                                            <a href="/classes" class="btn btn-primary">Browse Classes</a>
                                        </div>
                                    <% } %>
                                </div>
                            </div>
                        </div>

                        <!-- Available Classes Section -->
                        <div class="col-md-6">
                            <div class="card border-0 shadow-sm">
                                <div class="card-header bg-light">
                                    <h5 class="mb-0"><i class="bi bi-plus-circle me-2"></i>Available Classes</h5>
                                </div>
                                <div class="card-body">
                                    <% if (upcomingClasses && upcomingClasses.length > 0) { %>
                                        <% upcomingClasses.forEach(function(classItem, index) { %>
                                            <% if (index < 3) { %>
                                                <div class="d-flex justify-content-between align-items-center border-bottom py-2">
                                                    <div>
                                                        <h6 class="mb-1 text-light"><%= classItem.class_name %></h6>
                                                        <small class="text-muted">
                                                            <i class="bi bi-person me-1"></i><%= classItem.instructor_name %>
                                                        </small><br>
                                                        <small class="text-muted">
                                                            <i class="bi bi-clock me-1"></i><%= new Date(classItem.class_start_time).toLocaleDateString() %>
                                                        </small>
                                                    </div>
                                                    <div class="text-end">
                                                        <span class="badge bg-info mb-1"><%= classItem.available_spots %> spots</span><br>
                                                        <button class="btn btn-sm btn-primary">Book</button>
                                                    </div>
                                                </div>
                                            <% } %>
                                        <% }); %>
                                        <div class="text-center mt-3">
                                            <a href="/classes" class="btn btn-outline-success">View All Classes</a>
                                        </div>
                                    <% } else { %>
                                        <div class="text-center py-4">
                                            <div class="display-6 text-muted mb-3">🏋️</div>
                                            <h6 class="text-muted">No upcoming classes</h6>
                                            <p class="text-muted">Check back later for new classes!</p>
                                        </div>
                                    <% } %>
                                </div>
                            </div>
                        </div>
                    </div>

                    <!-- Quick Actions & Recent Activity -->
                    <div class="row g-4 mt-2">
                        <!-- Quick Actions -->
                        <div class="col-md-6">
                            <div class="card border-0 shadow-sm">
                                <div class="card-header bg-light">
                                    <h5 class="mb-0"><i class="bi bi-lightning me-2"></i>Quick Actions</h5>
                                </div>
                                <div class="card-body">
                                    <div class="row g-3">
                                        <div class="col-6">
                                            <a href="/classes" class="btn btn-outline-primary w-100">
                                                <i class="bi bi-plus-circle me-2"></i>Book Class
                                            </a>
                                        </div>
                                        <div class="col-6">
                                            <a href="/locations" class="btn btn-outline-success w-100">
                                                <i class="bi bi-geo-alt me-2"></i>Find Location
                                            </a>
                                        </div>
                                        <div class="col-6">
                                            <a href="/bookings" class="btn btn-outline-warning w-100">
                                                <i class="bi bi-calendar me-2"></i>My Schedule
                                            </a>
                                        </div>
                                        <div class="col-6">
                                            <a href="/billings" class="btn btn-outline-info w-100">
                                                <i class="bi bi-credit-card me-2"></i>Billing
                                            </a>
                                        </div>
                                    </div>
                                </div>
                            </div>
                        </div>

                        <!-- Recent Billing -->
                        <div class="col-md-6">
                            <div class="card border-0 shadow-sm">
                                <div class="card-header bg-light">
                                    <h5 class="mb-0"><i class="bi bi-receipt me-2"></i>Recent Billing</h5>
                                </div>
                                <div class="card-body">
                                    <% if (billings && billings.length > 0) { %>
                                        <% billings.forEach(function(bill) { %>
                                            <div class="d-flex justify-content-between align-items-center border-bottom py-2">
                                                <div>
                                                    <h6 class="mb-1 text-light">$<%= bill.total_amount %></h6>
                                                    <small class="text-muted">
                                                        <i class="bi bi-calendar me-1"></i><%= new Date(bill.billing_date).toLocaleDateString() %>
                                                    </small>
                                                </div>
                                                <span class="badge bg-<%= bill.payment_status === 'Paid' ? 'success' : 'warning' %>">
                                                    <%= bill.payment_status %>
                                                </span>
                                            </div>
                                        <% }); %>
                                        <div class="text-center mt-3">
                                            <a href="/billings" class="btn btn-outline-warning">View All Bills</a>
                                        </div>
                                    <% } else { %>
                                        <div class="text-center py-4">
                                            <div class="display-6 text-muted mb-3">💳</div>
                                            <h6 class="text-muted">No billing history</h6>
                                            <p class="text-muted">Your payment history will appear here</p>
                                        </div>
                                    <% } %>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </body>
</html>
